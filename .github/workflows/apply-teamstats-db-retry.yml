name: Apply teamstats DB retry patch

on:
  push:
    branches:
      - main
    paths:
      - scripts/apply-teamstats-db-retry-patch.mjs
      - .github/workflows/apply-teamstats-db-retry.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    if: ${{ !contains(github.event.head_commit.message || '', 'Add retry for transient teamstats DB sync errors') }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Apply patch
        run: node scripts/apply-teamstats-db-retry-patch.mjs

      - name: Remove temporary patch files
        run: |
          rm scripts/apply-teamstats-db-retry-patch.mjs
          rm .github/workflows/apply-teamstats-db-retry.yml

      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/update-teams-v2.js scripts/apply-teamstats-db-retry-patch.mjs .github/workflows/apply-teamstats-db-retry.yml
          git commit -m "Add retry for transient teamstats DB sync errors"
          git push
